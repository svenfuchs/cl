#!/usr/bin/env ruby
$: << File.expand_path('../../lib', __FILE__)

require 'erb'

def example(path)
  str = File.read("examples/#{path}")
  str.lines[5..-1].join
end

erb = ERB.new(File.read('var/README.erb.md'), nil, '-')
puts erb.result
